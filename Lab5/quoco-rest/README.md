To run this solution first run the docker-compose using the below command:

```docker-compose up --build```

The run the client to send clients to the broker and print out quotations:

```mvn exec:java -pl client```